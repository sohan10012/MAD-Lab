<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PassApp">

        <!-- 1️⃣ AuthActivity is the FIRST screen -->
        <activity
            android:name=".AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2️⃣ Main Activity after authentication -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- 3️⃣ Add or Edit password screen -->
        <activity
            android:name=".AddPasswordActivity"
            android:exported="false" />

        <!-- 4️⃣ Password Generator screen -->
        <activity
            android:name=".GeneratorActivity"
            android:exported="false" />

    </application>

</manifest>
